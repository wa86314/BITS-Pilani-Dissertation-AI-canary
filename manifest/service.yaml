apiVersion: v1
kind: Service
metadata:
  name: dissertation-ai
  namespace : bits-dissertation
spec:
  type: LoadBalancer
  selector:
    app: dissertation-ai
  ports:
    - port: 8888
      targetPort: 8888
