apiVersion: v1
kind: Service
metadata:
  name: dissertation-ai-canary
  namespace : bits-dissertation
spec:
  type: LoadBalancer
  selector:
    app: dissertation-ai-canary
  ports:
    - port: 8888
      targetPort: 8888
