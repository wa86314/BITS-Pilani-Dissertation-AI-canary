apiVersion: v1
kind: Service
metadata:
  name: dissertation-ai-canary
  namespace : bits-dissertation
spec:
  type: LoadBalancer
  selector:
    app: dissertation-ai-canary
  ports:
    - port: 80
      targetPort: 8888
